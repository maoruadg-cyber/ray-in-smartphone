# 工事日報システム

## 日報の保存

ユーザーが工事の内容を話したら、以下の手順で保存する：

1. `reports/YYYY-MM-DD.md` に追記（同じ日付ならファイルに追記、なければ新規作成）
2. ファイルには話された内容をそのまま残す。加工・省略しない
3. 保存後、`git add reports/ && git commit -m "日報追加: YYYY-MM-DD" && git push -u origin claude/notion-integration-check-jh2S3` を実行して同期する

### ファイル形式

```
## HH:MM 記録

（話された内容をそのまま）
```

---

## 情報の照会

ユーザーが「〇〇を教えて」「〇〇の件どうだっけ」のように聞いてきたら：

1. `reports/` 以下のファイルをすべて読む
2. 関連する記録をまとめて回答する
3. 現場名・会社名・人数・メンバー・作業内容などを整理して提示する

---

## 既存データ

作業員名簿・週報はすでに `workers/` フォルダに保存済み。

- `workers/roster.json` — 作業員名簿（57名、R7.11.19〜R8.5.11）
- `workers/weekly_reports.json` — 工事週報（第1〜5週、2026年5月）
- `workers/daily_records.json` — 日付別作業記録（2026年5月、18日分）

照会時はこれらのファイルも読んで回答する。

---

## 作業日報メニュー

ユーザーが「作業日報」とだけ言ったら、以下のメニューを表示する：

```
【作業日報 メニュー】

① 日付指定　　　→ 例：「5月25日誰が何してた？」
② 会社別実績　　→ 例：「バンゼイドの5月の作業を教えて」
③ 人物検索　　　→ 例：「板賀が入った日を全部教えて」
④ 現場別作業　　→ 例：「猪谷SSで何をやった？」
⑤ 作業内容検索　→ 例：「剥離材の作業をまとめて」
⑥ 人工集計　　　→ 例：「各社の月間人工数を教えて」
⑦ メンバー確認　→ 例：「R8.1.16の名簿を見せて」
⑧ 日報を記録　　→ 例：「今日の作業を記録して」
```

---

## 注意

- フォーマットは強制しない。話された言葉をそのまま保存することを優先する
- 日付が不明な場合は今日の日付を使う
- 記録の重複は気にしない。追記でどんどん溜めていく
